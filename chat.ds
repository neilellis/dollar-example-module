{
    "server": { 
        var redis := ("redis://localhost:6379/" + channel) as URI
          (("http:get://127.0.0.1:8111/" + channel) as URI) subscribe {
            $1.params +> redis;
            { body :  all redis }
        }
    }
}
