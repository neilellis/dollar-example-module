{
    server: $channel /*{ 
        redis := uri ("redis://localhost:6379/" + $channel)
        uri ("http:get://127.0.0.1:8111/" + $channel) subscribe {
            $1.params send $redis
            { body : receive all $redis }
        }
    }*/
}
